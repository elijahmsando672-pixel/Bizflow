{
  "name": "bizflow-sqlserver-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-off SQL Server to PostgreSQL data migration for the legacy BizFlow Express backend",
  "scripts": {
    "plan": "node bin/migrate.js plan",
    "migrate": "node bin/migrate.js migrate",
    "verify": "node bin/migrate.js verify",
    "fixture": "node scripts/build-fixture.js",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mssql": "^12.7.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
