-- 001_initial_schema.sql
-- Baseline: all tables from schema init. This is the reference state.
-- Actual table creation happens in config/db.js; this migration records the baseline.
-- No SQL needed here — the schema is applied by initDatabase().
SELECT 1;
