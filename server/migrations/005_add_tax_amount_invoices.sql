-- 005_add_tax_amount_invoices.sql
-- Postgres: added invoices.tax_amount plus missing indexes.
-- For SQL Server: invoices.tax_amount and the additional indexes are defined in
-- config/schema.tsql.js (post-initial additions block at the end).
-- Recorded here to keep the migrations ledger consistent. No SQL needed.
SELECT 1;